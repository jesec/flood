export function searchTorrents(torrentList, torrentListSearchString) {
  let searchedTorrents = torrentList;

  if (torrentListSearchString !== '') {
    // search the torrent name
  }

  return torrentList;
}
